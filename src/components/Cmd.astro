---
import Window from "./Window.astro";
interface Props {
    title?: string;
    width?: string;
}

const { title = "Command Prompt", width = "700px" } = Astro.props;
---

<Window title=`${title}` width=`${width}`>
    <slot />
</Window>
