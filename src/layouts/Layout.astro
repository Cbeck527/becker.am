---
import "xp.css/dist/XP.css";
---

<!doctype html>
<html lang="en">
    <head>
        <title>chris becker</title>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="canonical" href="https://www.becker.am/" />
        <meta property="og:title" content="chris becker" />
        <meta name="Description" content="personal site for chris becker" />
        <meta property="og:description" content="personal site for chris becker" />
        <meta property="og:image" content="https://www.becker.am/assets/og-image.png" />
        <meta name="theme-color" content="#0078d7" />
        <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
        <meta name="apple-mobile-web-app-capable" content="yes" />
        <meta name="generator" content={Astro.generator} />
    </head>
    <body>
        <main>
            <slot />
        </main>
    </body>
</html>

<style>
    html {
        height: 100%;
    }

    body {
        margin: 0;
        padding: 0;
        padding-top: env(safe-area-inset-top);
        padding-right: env(safe-area-inset-right);
        padding-bottom: env(safe-area-inset-bottom);
        padding-left: env(safe-area-inset-left);
        min-height: 100%;
        height: 100%;
        background-image: url("/bliss.jpg");
        background-position: center center;
        background-repeat: no-repeat;
        background-size: cover;
        background-attachment: fixed;
        position: relative;
    }

    main {
        margin: 20px 200px;
        display: flex;
        flex-wrap: wrap;
        gap: 12px;
    }
</style>
