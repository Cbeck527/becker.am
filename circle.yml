machine:
  ruby:
    version: ruby-2.2.3

# SOON :D
# general:
#   artifacts:
#     - "./_site"

test:
  override:
    - bundle exec jekyll build
    - bundle exec htmlproof ./_site/ --disable-external --check-html
